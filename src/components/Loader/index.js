import React from 'react'
import {
    CircularProgress 
} from '@material-ui/core'

const PerLoader = () => {
    return(
        <React.Fragment>
            <CircularProgress />
        </React.Fragment>
    );
}

export default PerLoader;